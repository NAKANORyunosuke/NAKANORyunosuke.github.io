{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run run.bat",
            "type": "node-terminal",
            "request": "launch",
            "command": ".\\run.bat",
            "cwd": "${workspaceFolder}"
        }
    ]
}
